name: test workflow

on:
  push:
    branches:
      - main

jobs:
    integration:
        runs-on: ubuntu-latest

        steps: 
            - name: Test workflow
              run: echo "Test this workflow"